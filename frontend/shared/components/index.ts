/**
 * Shared components barrel export.
 *
 * UI primitives live in ./ui/ and follow the RDS design system.
 * Import from '@/shared/components/ui' for individual components,
 * or from '@/shared/components' for the full set.
 */
export * from './ui';
